function structure() {
	var person = {
		firstName : "Mathieu",
		lastName : "MARTINS RIBEIRO",
		age : 20,
		eyeColor : "Blue"
	};

	document.write("Full Name : " + person.firstName + " " + person.lastName + '<br/>' + "Age : " + person.age + '<br/>' + "Eyes : " + person.eyeColor);
}